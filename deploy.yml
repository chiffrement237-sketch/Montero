name: Oliver Montero Workflow

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout de Montero
        uses: actions/checkout@v4

      - name: Démarrage de Oliver Montero 
        run: |
          echo "Initialisation du projet par Montero..."
          echo "Oliver Montero est prêt à être déployé."
